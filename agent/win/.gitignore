# runtime artifacts — never committed (package-lock.json IS committed for `npm ci`)
node_modules/
*.state.json
*.log
drive-evidence.txt
probe-result.txt
# the drive's throwaway live-token file (section [9], the XERK-578 roll)
*.token
