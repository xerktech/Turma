{
  "name": "@turma/dsh-session-driver",
  "version": "0.1.0",
  "description": "Per-session dsh driver plugin: binds a Turma control socket, runs one agent, writes the native event log for the transcript projection (XERK-467 [C]).",
  "type": "module",
  "main": "dist/index.js",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": "*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.yml"
    }
  }
}
