# Turma native agent configuration.
#
# Plain KEY=value (no `export`, no shell expansion, no $HOME in values). Loaded
# both by systemd (EnvironmentFile) and by the launcher (`set -a; . file`). The
# launcher fills HOME-relative defaults for any key left BLANK, which is the
# recommended way to set the paths — don't hardcode /home/<you> here.
#
# install.sh writes this once and never overwrites it, so your edits survive
# reinstalls and updates.

# --- Required -------------------------------------------------------------
# Hub base URL. The agent connects purely OUTBOUND, so this is the public
# Cloudflare-tunnel URL (works from any network).
TURMA_URL=https://turma.example.com
# This host's agent credential. Start with the hub's shared TURMA_AGENT_TOKEN,
# then move onto this host's OWN derived token — no hand-edit needed: click "Roll
# token" on the dashboard, run `turma-agentctl enroll`, or set the self-enroll
# flag below (XERK-578). Also used as the per-session ttyd basic-auth password.
# Keep this file private (chmod 600).
TURMA_TOKEN=
# Host key the hub identifies this agent by. install.sh seeds $(hostname). The
# derived token is tied to this name, so changing it re-triggers a roll/enroll.
DEVICE_NAME=
# Zero-touch onboarding (XERK-578): set to 1 to roll onto the derived token on
# every start (idempotent, best-effort, never blocks start). Unset = off.
# TURMA_AGENT_SELF_ENROLL=1

# --- Paths (leave BLANK to accept the launcher's HOME-relative defaults) ---
# Git root scanned one level deep for repos; worktrees live under it. Default: $HOME/git
REPOS_ROOT=
# Where Claude Code keeps transcripts. Default: $HOME/.claude/projects
# (MUST NOT be /root/.claude/projects on a native install.)
CLAUDE_PROJECTS_ROOT=

# --- Tuning (code defaults shown) -----------------------------------------
MAX_SESSIONS=6
TTYD_PORT_BASE=7700
TURMA_INTERVAL=20
# Root install only (e.g. a TrueNAS appliance): Claude Code refuses
# --permission-mode bypassPermissions under root unless this is set — without
# it such sessions die on spawn with exit 1. Same opt-in the container compose
# makes. Leave unset on a normal per-user install.
# IS_SANDBOX=1

# --- Auto-update (optional) -----------------------------------------------
# The agent checks for a newer agent build on every start and then hourly, and
# for a newer Claude Code on every start (only there — replacing it while
# sessions can be launching would break them). Override the source, the cadence,
# or opt out entirely:
# TURMA_REPO=xerktech/turma
# TURMA_UPDATE_INTERVAL=3600
# GH_TOKEN=            # only if `gh` isn't logged in on this host
# TURMA_BOOT_UPDATE=0             # check nothing on start. NOTE: Claude Code is
#                                 # checked ONLY on start, so this also stops it
#                                 # being updated at all; the hourly timer keeps
#                                 # updating the agent itself.
# TURMA_BOOT_UPDATE_MIN_INTERVAL=300   # floor between start-triggered checks
# TURMA_CLAUDE_AUTO_UPDATE=0      # leave Claude Code at whatever is installed
# TURMA_CLAUDE_UPDATE_TIMEOUT=1800  # the most a start may wait on that check.
#                                 # A WORST CASE, not what a start costs: with
#                                 # nothing new published the check is two
#                                 # version reads, well under a second. It is
#                                 # held clear of the install budget below (2.5x
#                                 # it), because cutting the check off mid-install
#                                 # leaves npm replacing claude while sessions
#                                 # relaunch into it.
# TURMA_NPM_INSTALL_TIMEOUT=300   # the big one; covers BOTH install attempts
# TURMA_NPM_VIEW_TIMEOUT=45       # the "what's published?" read
# TURMA_CLAUDE_PROBE_TIMEOUT=30   # each `claude --version`
# TURMA_NPM_META_TIMEOUT=30       # `npm prefix -g` / `npm ls -g`
# TURMA_KILL_GRACE=10             # SIGTERM -> SIGKILL grace for the above; without
#                                 # it `timeout` only signals and then waits
# TURMA_DSH_UPDATE_TIMEOUT=1800   # the most a start may wait on the dsh CLI check
#                                 # (the same bound claude gets; only a dsh host runs it)

# --- Optional integrations ------------------------------------------------
# GH_CLONE_OWNERS=              # extra owners the hub may clone from (space/comma)
# GitLab as an extra clone source (XERK-155): the token LISTS the projects the
# account is a member of; clones go over SSH, so this host's ssh key must be
# authorized on the GitLab host (and its host key known).
# GITLAB_URL=                   # https://gitlab.com or a self-hosted base
# GITLAB_TOKEN=                 # PAT with read_api
# (An AZDO_URL/AZDO_TOKEN below also lists that org's repos as a clone source.)
# A host polls ONE ticket source. Jira Cloud:
# JIRA_SITE=                    # your-org.atlassian.net
# JIRA_EMAIL=
# JIRA_TOKEN=
# ...or Azure DevOps (self-hosted or Services); AZDO_* wins if both are set:
# AZDO_URL=                     # https://dev.azure.com/org, or a self-hosted
#                               # https://tfs.company.com/tfs/DefaultCollection.
#                               # INCLUDE THE SCHEME: a bare host is assumed
#                               # https, which then won't match a PR URL on a
#                               # plain-http on-prem server.
# AZDO_TOKEN=                   # PAT with Work Items (Read)
# AZDO_PROJECT=                 # optional: scope to one project (else org-wide)
# AZDO_USER=                    # optional: display-only, for the board's merge field
# AZDO_API_VERSION=6.0          # self-hosted trails cloud: 7.0 = Server 2022,
#                               # 6.0 = 2020, 5.0 = 2019, 4.1 = TFS 2018. An
#                               # out-of-range version is answered with a 400.
# BOARD_ORG_NAME=               # the board's org LABEL, overriding the name
#                               # derived from the site: a self-hosted collection
#                               # derives to "defaultcollection", its deployment
#                               # detail rather than the org. Presentational only
#                               # — the siteKey routing and the hub's ledgers key
#                               # on is untouched, so this is safe to change later.

# --- PR chips ---------------------------------------------------------------
# A session's PR chip is attributed from the command that OPENED the PR, so a
# host that opens PRs with its own wrapper has to name it here, else its cards
# never chip. `gh pr create`, `glab mr create`, `az repos pr create` and
# `ado pr-create` (also as `ado.py`) are built in and need no entry.
# Comma-separated command prefixes, matched literally; an entry whose longest
# word is under 3 characters is ignored — it would match half of what a session
# runs, and attribution must not fail open:
# TURMA_PR_CREATE_CMDS=mkpr,tfs pr new

# --- dsh runtime (DeepSeek Harness) -----------------------------------------
# dsh is an OPTIONAL per-session runtime alongside Claude Code (XERK-460). A host
# must ship the dsh toolchain (CLI + driver/guard + python siblings) — install.sh
# --with-dsh does that AND seeds TURMA_DSH=1 here — and only then does this
# switch make the host OFFER dsh. Setting TURMA_DSH=1 without the toolchain in
# place leaves every dsh spawn refused, so prefer `install.sh --with-dsh` over
# flipping this by hand:
# TURMA_DSH=1
# A dsh session has NO Claude failover: it reaches a model only through the route
# below (dsh falls back to the LOCAL_MODEL_* keys when the DSH_* ones are unset).
# DSH_MODEL_BASE_URL=            # OpenAI-compatible base, e.g. a LiteLLM /v1 URL
# DSH_MODEL=                     # model id dsh should use
# DSH_MODEL_API_KEY_ENV=LOCAL_MODEL_API_KEY   # name of the env var holding the key
# DSH_MODEL_CONTEXT=200000       # context window; inherits LOCAL_MODEL_CONTEXT

# --- default runtime for unpinned work (XERK-521) ---------------------------
# Which runtime an UNPINNED spawn runs on: an auto-started/unpinned ticket
# session, and a bare "+ New session" whose Runtime dropdown was never touched.
# An explicit composer pick or a per-ticket runtime pin always wins over this.
# One of {claude,dsh,qwen}. UNSET -> claude, so every current host is unchanged.
# Self-validating: if this names a runtime the host has NOT configured (its kill
# switch off, or TURMA_DSH / TURMA_QWEN and the toolchain not in place), the host
# falls back to claude and says so — it never advertises or launches a runtime it
# cannot run. So set this only where you have ALSO enabled that runtime above.
# TURMA_DEFAULT_RUNTIME=qwen

