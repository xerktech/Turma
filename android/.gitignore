.gradle/
build/
/local.properties
*.iml
.idea/
.DS_Store
captures/
.externalNativeBuild/
.cxx/

# Gradle wrapper jar/scripts are generated in CI (`gradle wrapper`); the pinned
# version lives in gradle/wrapper/gradle-wrapper.properties.
gradle/wrapper/gradle-wrapper.jar
gradlew
gradlew.bat

# Firebase config — per-project secret, dropped in by the operator (see README).
google-services.json

# Signing
*.keystore
*.jks
keystore.properties
# ...except the committed release keystore that gives every build a stable
# signing cert so the app can update in place (XERK-26). It is intentionally
# in the repo — see the signingConfigs note in app/build.gradle.kts.
!/app/turma-release.keystore

# Kotlin incremental compile cache
.kotlin/
