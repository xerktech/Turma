{
  "name": "@turma/poc-turma-2.0",
  "version": "0.0.1",
  "private": true,
  "description": "Proof of concept for Turma 2.0 multi-host dsh architecture",
  "scripts": {
    "hub": "cd fleet-hub && npm run dev",
    "test": "./test-real-dsh.sh",
    "test:mock": "npm exec -- tsx test-standalone.ts",
    "test:mock-multi": "npm exec -- tsx test-multi-host.ts",
    "typecheck": "cd fleet-hub && npm exec -- tsc --noEmit -p . && cd ../fleet-agent-plugin && npm exec -- tsc --noEmit -p .",
    "setup": "cd fleet-hub && npm install && cd ../fleet-agent-plugin && npm install"
  },
  "devDependencies": {
    "@types/ws": "^8.5.10",
    "tsx": "^4.7.0",
    "ws": "^8.16.0"
  },
  "dependencies": {
    "@deepseek-ai/dsh": "^0.1.1-rc.2"
  }
}
