__pycache__/
*.pyc

# Local compose secrets — never commit
.env

# Packed glasses app — built by `glasses` npm run pack, published to the Even
# Hub portal (not committed, not a release asset).
/turma.ehpk

# Ephemeral git worktrees created by Claude Code subagents running with
# `isolation: "worktree"`. They are real nested checkouts, so a `git add -A`
# stages them as embedded repositories — which very nearly happened. Nothing
# here is ever meant to be committed.
/.claude/worktrees/
